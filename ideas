import c3js
  bower install c3


stream live data
https://developer.tradier.com/documentation/markets/create-events-session
  data:
    stream: 
      sessionid:
      url:
        ATT_BLOCK:
        TEXT:

nodemon --inspect won't work if static files isn't served as (__dirname + '/somepathtosomefile');



// search function
// A stock page that renders for a specific stock
// watchlist
